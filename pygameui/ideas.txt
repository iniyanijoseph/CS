Have button for new sprite object. + sign leading to searchbar where can edit the class.
    - Collision
    - New script = new class where check boxes can add new buttons
	- Pyganim Support?

Have button for new general class. + sign for draw function, etc.
    - New script = new function in class

On uiside:
	for each in classlist, display check boxes
	barebones text editor in uiside else, button to lanuch from dropdownmenu